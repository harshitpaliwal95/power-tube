import React from "react";

const exploreReducer = (state, { type, payload }) => {
  switch (type) {
    case type:
      return state;

    default:
      return state;
  }
};
export { exploreReducer };
